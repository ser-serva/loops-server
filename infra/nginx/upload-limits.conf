# Override client_max_body_size at the server block level (takes precedence over
# the 100M set in nginx.conf's http block).
client_max_body_size 500M;

# Give PHP-FPM enough time for large uploads
fastcgi_read_timeout 300s;
